<div class="navbar navbar-default navbar-top sit-on-top" role="navigation">
  <div class="container top-header">
    <div class="navbar-header">
      {{# link-to 'home' class='navbar-brand' }}
      <img class="brand-logo img-dp-shadow" src="{{rootURL}}images/oceans.png" height="75">
      {{/ link-to }}
      <div class='navbar-brand'>
      {{# link-to 'home' class='navbar-brand' }}
        <h4>{{ t 'header.title' }}</h4>
      {{/ link-to }}
      </div>
    </div>
    <div class='collapse navbar-collapse'>
      <ul class='nav navbar-nav navbar-right pad-top-10'>
        <ul class='nav navbar-nav navbar-right'>
          <!-- {{#if session.shouldRage}}
            <li><a href="#">Rage</a></li>
          {{else}}
            <li><a href="#">Chill</a></li>
          {{/if}} -->
          {{ago-signin currentUrl=applicationController.currentAppUrl  }}
        </ul>
      </ul>
    </div>
  </div>
</div>

{{outlet}}
